@layout({title:movie.title})

<h1>{{movie.title}}</h1>

@if (movie.abstract)
<div class="my-8 bg-slate-100 rounded-xl p-8">
  {{{ movie.abstract }}}
</div>
@else
No Abstract
@endif

{{inspect({movie,globalExample})}}

@end