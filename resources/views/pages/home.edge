<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>
    AdonisJS - Movies
  </title>




  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>

<body>
  <div class="max-w-3xl mx-auto mt-6">

    <h1 class="text-red-500">It Works !!</h1>

    <ul>
      @each(movie in movies)
      <li>
        <a class="text-blue-500 hover:text-blue-700 underline" href={{ route('movies.show',{slug:movie.slug})}}>{{
          movie.title }}</a>
      </li>
      @end
    </ul>

    {{ inspect(movies) }}

  </div>
</body>

</html>