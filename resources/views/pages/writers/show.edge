@layout()
<h1>Writer {{writer.fullName}}</h1>

<div class="flex flex-wrap -mx-6">
    @each(movie in writer.moviesWritten)
    @!movie.card({movie, class: 'w-1/3' })
    @end
</div>

{{ inspect(writer)}}

@end